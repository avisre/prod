{
  "name": "stockportfolio-mcp",
  "version": "0.2.0",
  "description": "Verified Financial Data MCP Server — thin wrapper over StockPortfolio.pro filing-grounded tools, every value ships with its SEC source.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test test/*.test.js"
  },
  "keywords": ["mcp", "sec", "filings", "financial-data"],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.0"
  }
}
