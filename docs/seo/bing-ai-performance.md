# Bing/Copilot citation performance

The repository currently contains no Bing AI Performance or citation export. The CSV is intentionally header-only; no citation, query, ticker, or metric is inferred from ordinary Bing clicks. Populate it only from an authenticated export/API response, then rerun the page-family analysis.
