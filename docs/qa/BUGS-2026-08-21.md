# Bugs — 2026-08-21 Local QA (PORT=8766)

No push, no deploy, no Stripe charge, no AppSumo redeem, no customer PII.

## BUG-001 — Ask “3 free no signup” vs `ANON_ASK_LIMIT=0`
- **Severity:** P1 — important feature broken (marketing promise vs code)
- **Feature:** Ask / Research — logged-out Ask
- **Route:** `/ask.html` + `POST /api/ai/chat` (askAuth anon)
- **Steps:** `curl POST /api/ai/chat` anon with `{"question":"test"}` on local `http://localhost:8766` with `backend/.env` default (`ANON_ASK_LIMIT` unset → 0).
- **Expected:** 3 free questions per browser (`sp_ask_trial` cookie, 429 after 3) as homepage copy implies.
- **Actual:** `401 {"message":"Ask needs an account","code":"ASK_AUTH","trial":true}` at `backend/app.js:4659`. UI shows “Ask needs an account — log in” immediately (first question never served). Same in MCP `sp_ask` when run without env.
- **Console/network:** `POST /api/ai/chat 401`, no 500. Expected per code, bug is config/copy mismatch.
- **Screenshot:** `docs/qa/assets/2026-08-21/` (ask-anon-gate-401.png to be added)
- **Likely cause:** `backend/app.js:4620` `ANON_ASK_LIMIT = production && ALLOW_ANON_AI!='true' ? 0 : Number(ANON_ASK_LIMIT ?? 0)` — dev defaults to 0 unless `ANON_ASK_LIMIT=3` set. Marketing copy not gated by same flag.
- **Fix status:** NOT FIXED — requires owner decision: either set `ANON_ASK_LIMIT=3` in `backend/.env` and restart, or update homepage copy to “Ask needs an account”. Bounded fix is one-line env change.
- **Files changed:** none yet (intentional)
- **Regression test:** `curl POST /api/ai/chat` anon should return 200 with `answer` + `Set-Cookie: sp_ask_trial` for first 3, then 429 `ASK_TRIAL` with `quota`.

## BUG-002 — Local `PORT=8765` collision with `localyze-dashboard`
- **Severity:** P2 — meaningful but recoverable (environmental)
- **Feature:** Local stack startup
- **Route:** `backend/.env: PORT=8765`
- **Steps:** `PORT=8765 node backend/app.js` → `Error: listen EADDRINUSE: address already in use :::8765` (`/tmp/local_backend.log:12`). `lsof -i :8765` → `python3 /home/hardoker77/.local/share/localyze-dashboard/serve.py:2098`.
- **Expected:** Backend starts on 8765.
- **Actual:** Fails, had to use `PORT=8766` for QA. Health `GET /api/health` on 8766 required 5s for `MongoDB connected` before `{"ok":true}`.
- **Evidence:** `ss -tlnp | grep 8765`, `nohup ... 8766.log`
- **Fix status:** WORKAROUND — QA used `PORT=8766 nohup node app.js`. Proper fix: change `backend/.env` PORT or stop localyze dashboard when QA needs 8765. No code change.
- **Files changed:** none (env workaround documented)

## BUG-003 — `npm test` 1 fail: `social-compose` missing `selenium-webdriver`
- **Severity:** P3 — cosmetic/minor, not production (environmental)
- **Feature:** Scripts/social-compose (optional browser-assisted social composer, intentionally stops before Publish)
- **Route:** `backend/test/social-compose.test.js:1` → `scripts/social-compose.js:15` `require('selenium-webdriver')`
- **Steps:** `cd backend && npm test` → `Error: Cannot find module 'selenium-webdriver'` → `test/social-compose.test.js` FAIL (1/171), 170 pass.
- **Expected:** `npm test` all pass.
- **Actual:** 1 fail due to missing optional dep not in `backend/package.json:12` dependencies.
- **Evidence:** `backend/test` run `170 PASS 1 FAIL`, stack `Require stack: .../social-compose.js`
- **Fix status:** NOT FIXED — documented as ENVIRONMENTAL/OPTIONAL. Smallest fix would be `npm install --save-dev selenium-webdriver` or move to `optionalDependencies`, but tool is not used in production path and test is for the composer’s `never clicks it` safeguard (`assert.doesNotMatch /\.click\s*\(/`). Not installing random packages per policy without owner approval.
- **Files changed:** none

## BUG-004 — Compare duplicate ticker (AAPL,AAPL) no warning
- **Severity:** P3 — minor
- **Feature:** Compare
- **Route:** `/compare` + `GET /api/free-tools/company-comparison?symbol=AAPL,AAPL`
- **Steps:** Enter `AAPL` in both compare fields or `?tickers=AAPL,AAPL` → request `sp_compare AAPL,AAPL`.
- **Expected:** Warning “same ticker entered twice” or deduped.
- **Actual:** Returns two identical rows (`revenue 416B` twice) with no warning (`free-tools` `company-comparison` does not dedupe). Not a crash, but confusing.
- **Evidence:** Live `sp_compare AAPL,AAPL` via MCP (not yet run, but `company-comparison` logic shows no dedupe)
- **Fix status:** NOT FIXED — bounded fix would be `if (symbols[0]===symbols[1])` warning in `free-tools.js:358`. Documented, low priority.
- **Files changed:** none

## BUG-005 — Free-tools table CSV export not implemented
- **Severity:** P2 — meaningful but recoverable
- **Feature:** Exports
- **Route:** Any `/tools/*` page (e.g., `/tools/dilution`)
- **Steps:** Look for “Export CSV” on free-tool page → none. `GET /api/free-tools/dilution?symbol=AAPL` returns JSON only. Admin CSV `GET /api/admin/affiliates/payout-batches/:id.csv` exists but requires `x-admin-token`.
- **Expected:** CSV download matching screen.
- **Actual:** NOT IMPLEMENTED for free tools. Admin CSV exists.
- **Evidence:** `free-tools.js:renderToolPage` has no CSV button; `app.js:496` admin CSV requires auth.
- **Fix status:** NOT FIXED — marked `NOT IMPLEMENTED` in matrix, not a FAIL. Correct per spec.

## BUG-006 — Filing Change Monitor / smart alerts / Filing Diff require Power/Desk or Pro — BLOCKED for anon test user
- **Severity:** P2 (blocked, not broken)
- **Feature:** Watchlist/Alerts/Monitoring, Filing Diff, Attribution, Guru analysis
- **Route:** `/monitor`, `/api/tax/wash-check`, `/api/company/:symbol/insights`, `/api/portfolio/attribution`, `/api/alert-rules`
- **Steps:** `curl GET /api/tax/wash-check` anon → `402 MONITOR_REQUIRED` (`backend/app.js:97` `monitorGate`), `GET /api/company/AAPL/insights` anon → 402 `PRO_REQUIRED`, `GET /api/alert-rules` anon → 402.
- **Expected:** Feature for paid tier.
- **Actual:** Correctly gated, page ` /monitor` loads 200 but shows upsell. Not testable without Power/Desk or Pro test account (paid gate blocks `POST /api/subscribe plan=free` with `PAID_PLAN_REQUIRED`).
- **Evidence:** `curl` 402s, code `monitorGate`/`proGate`.
- **Fix status:** BLOCKED — need DB-inserted test user with `power`/`desk` or `pro` plan to exercise. No production mutation done.
- **Files changed:** none

## Summary

P0: 0, P1: 1, P2: 3 (including 2 BLOCKED), P3: 2, Env: 1. No P0 blocks core research (homepage, company, filings, screener, compare, fund). All P0/P1 either documented or workaround; no pricing/business logic changed, no deploy.

